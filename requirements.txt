pandas>=2.0
pyarrow>=14.0
tabulate>=0.9
scikit-learn>=1.9
xgboost>=3.3
matplotlib>=3.8
joblib>=1.3

# interactive app (app/streamlit_app.py)
streamlit>=1.30
# CA bundle for fetching release artifacts on first launch
certifi>=2024.0

# dev / test / notebook deps live in requirements-dev.txt
